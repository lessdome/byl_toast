import 'dart:ui';

class UI {
  /// 屏幕宽度
  static double width = window.physicalSize.width;

  /// 屏幕宽度
  static double height = window.physicalSize.height;

  /// 默认弹窗最大宽度
  static double defaultToasMaxtWidth = UI.width * 0.8;
}
